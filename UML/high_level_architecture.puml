@startuml
!theme vibrant

title High-Level System Architecture

actor User

package "Browser" {
  [Frontend (HTML/JS/CSS)] as WebApp
}

package "Cloud Infrastructure" {
    [Backend API (FastAPI)] as Backend
    
    package "Supabase (BaaS)" {
      [Auth]
      [Database]
      [Storage]
    }
}

User -- WebApp : Uses

WebApp -> Backend : Makes API Calls (HTTPS)

Backend --> Database : Reads/Writes Data
Backend --> Auth : Verifies Users
Backend --> Storage : Manages Files

@enduml
